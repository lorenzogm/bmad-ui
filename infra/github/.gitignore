# Terraform state (never commit - contains sensitive data)
src/.terraform/
src/terraform.tfstate
src/terraform.tfstate.backup
src/terraform.tfstate*.json

# Provider lock file (auto-generated; commit in production if desired)
# src/.terraform.lock.hcl

# Local overrides
src/terraform.tfvars
src/*.auto.tfvars
