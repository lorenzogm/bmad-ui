dist
node_modules
.git-worktrees
logs
runtime-state.json

# dotenvx — commit encrypted .env, never commit decryption keys
.env.keys

# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
**/.terraform.tfstate.lock.info
crash.log
override.tf
override.tf.json
